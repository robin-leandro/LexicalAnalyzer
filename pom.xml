<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cr.ac.ucr.ci1322</groupId>
    <artifactId>examen-final</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <outputDirectory>src/main/java/</outputDirectory>
        <mainClass>cr.ac.ucr.ci1322.Main</mainClass>
        <!-- Autogenerated files by jFlex and Cup -->
        <queryLexicalAnalyzer>QueryLexicalAnalyzer</queryLexicalAnalyzer>
        <documentLexicalAnalyzer>DocumentLexicalAnalyzer</documentLexicalAnalyzer>
    </properties>

    <build>
        <plugins>

            <!-- jFlex plugin -->
            <plugin>
                <groupId>de.jflex</groupId>
                <artifactId>jflex-maven-plugin</artifactId>
                <version>1.6.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>

                </executions>
                <configuration>
                    <outputDirectory>${outputDirectory}</outputDirectory>
                </configuration>
            </plugin>

            <!-- Maven assembly to generate a jar containing every dependency -->
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${mainClass}</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
            </plugin>

            <!-- Clean plugin to delete .java's generated in the cr.ac.ucr.ci1322 package -->
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>${outputDirectory}/cr/ac/ucr/ci1322</directory>
                            <includes>
                                <include>${documentLexicalAnalyzer}.java</include>
                                <include>${queryLexicalAnalyzer}.java</include>
                                <include>sym.java</include>
                                <include>parser.java</include>
                                <include>LexerGrike.java</include>
                            </includes>
                            <followSymlinks>false</followSymlinks>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>

        </plugins>
    </build>



</project>